export type Post = {
    name: string;
    detail: string;
    link: string;
};

export type paragraph = string;

export type image = {
    boxSize?: string;
    src: string;
    alt?: string;
    link: string;
};
