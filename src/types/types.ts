export type Post = {
    title: string;
    date: string;
    author: string;
    description: string;
    imageUrl: string;
    link: string; // link to detail post
    tags?: string[];
};

export type paragraph = string;
